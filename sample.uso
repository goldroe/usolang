
struct Foo {
  String name;
}

union Vector2 {
  struct {
    float x, y;
  }
  struct {
    float u, v;
  }
}

factorial(num: int): int {
  if (num == 0) {
    return 1;
  }

  return num * factorial(num-1);
}

func(vec: Vector2): float, float {
  return vec.x, vec.y;
}

main(argc: int, argv: String): int {
  for x: 1..10 {
    if x < 10 {
      x++;
    }
    print("Num %\n", x);
  }

  return factorial(100);
}